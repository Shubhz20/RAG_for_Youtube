youtube-transcript-api
yt-dlp
openai
chromadb
streamlit
python-dotenv
# faster-whisper is optional — install locally for audio fallback transcription
# It doesn't build reliably on Streamlit Cloud, so it's commented out here
# faster-whisper
