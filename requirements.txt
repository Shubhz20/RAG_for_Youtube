yt-dlp
groq
chromadb
onnxruntime
streamlit
python-dotenv
# faster-whisper is optional — install locally for audio fallback transcription
# Commented out because it doesn't build on Streamlit Cloud
# faster-whisper
